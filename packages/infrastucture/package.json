{
  "name": "infrastructure",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "direnv exec . docker compose up -d",
    "start": "direnv exec . docker compose up -d",
    "stop": "docker compose down"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
